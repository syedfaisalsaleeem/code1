def factorial(x):
     y=x
     for i in range(1,x):
         x=x-1
         y=(y*(x))
     return(y)
fact=int(input("enter any number to calculate factorial="))
factorial=factorial(fact)
print("the calculated factorial is="+str(factorial))
